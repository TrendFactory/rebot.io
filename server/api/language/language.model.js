'use strict';

var mongoose = require('mongoose'),
    Schema = mongoose.Schema;

var LanguageRankSchema = new Schema({
  
});

module.exports = mongoose.model('Language_Rank', LanguageRankSchema, 'Language_Rank');
